import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coolieNo1Provider.app',
  appName: 'Coolie no.1 provider',
  webDir: 'dist'
};

export default config;
